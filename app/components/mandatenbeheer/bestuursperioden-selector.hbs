<PowerSelect
  @selected={{@selectedPeriod}}
  @options={{this.periodOptions}}
  class="is-optional"
  @onChange={{this.selectPeriod}}
  @searchEnabled={{false}}
  as |period|
>
  <Tmp::Periode
    @start={{period.startDate}}
    @end={{period.endDate}}
  >Bestuursperiode</Tmp::Periode>
</PowerSelect>