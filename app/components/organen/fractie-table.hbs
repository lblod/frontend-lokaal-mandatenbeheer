<AuDataTable
  @content={{@content}}
  @noDataMessage="Geen fracties gevonden"
  @sort={{@sort}}
  @page={{@page}}
  @size={{@size}}
  as |t|
>
  <t.content as |c|>
    <c.header>
      <AuDataTableThSortable
        @field="naam"
        @currentSorting={{@sort}}
        @label="Fractienaam"
      />
      <th>Bestuursperiode</th>
      <th></th>
    </c.header>

    <c.body as |row|>
      <td>
        <strong>{{row.naam}}</strong>
      </td>
      <td>
        {{@selectedPeriod.label}}
      </td>
      <td>
        <AuButton @skin="link" {{on "click" (fn @edit row.id)}}>
          <AuIcon @icon="pencil" @alignment="left" />
          Bewerk</AuButton>
      </td>
    </c.body>
  </t.content>
</AuDataTable>