{{#if this.globalSystemMessage.isActive}}
  <AuAlert
    @skin="warning"
    @size="tiny"
    @closable={{true}}
    class="au-u-margin-bottom-none"
    ...attributes
  >
    <p class="au-u-text-center">
      {{this.globalSystemMessage.message}}
    </p>
  </AuAlert>
{{/if}}