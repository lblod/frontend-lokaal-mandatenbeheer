{{page-title "Error " @model.status}}

<AuToolbar @border="bottom" @size="large" as |Group|>
  <Group>
    <AuHeading @skin="2">{{@model.status}}</AuHeading>
  </Group>
</AuToolbar>

<div class="au-c-body-container au-c-body-container--scroll">
    <div class="au-o-box">
      <AuAlert @icon="info-circle" @size="small">
        <p>{{@model.message}}</p>
      </AuAlert>
    </div>
</div>