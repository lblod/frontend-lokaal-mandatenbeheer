{{page-title "Instances"}}
<Form::InstanceTable
  @onCreate={{this.onCreate}}
  @form={{this.model.form}}
  @onRemoveInstance={{this.onRemoveInstance}}
  @editRoute="mandatenbeheer.fracties-new.instance"
/>
